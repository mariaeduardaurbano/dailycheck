generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model tarefas {
  id           Int      @id @default(autoincrement())
  nome         String
  dataComeco   String
  dataTermino  String
  descricao    String
  imagem       String
}